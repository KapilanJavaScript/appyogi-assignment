const CONTROL_TIMEOUT_MS = 120 * 1000; // 120 seconds
const COLOR = {
    'white': 1,
    'red': 2,
    'yellow': 3,
}
const CHECK_INTERVAL = 4000; // check every 4 seconds

export { CONTROL_TIMEOUT_MS, COLOR, CHECK_INTERVAL };